[source:local]
transport = local
database = ~/.cc-switch/cc-switch.db
# Optional: semicolon-separated directories; only session headers are read.
codex_session_roots = ~/.codex/sessions;~/.codex/archived_sessions
